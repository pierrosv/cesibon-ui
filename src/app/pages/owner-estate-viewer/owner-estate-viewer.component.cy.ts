import { OwnerEstateViewerComponent } from './owner-estate-viewer.component'

describe('OwnerEstateViewerComponent', () => {
  it('should mount', () => {
    cy.mount(OwnerEstateViewerComponent)
  })
})